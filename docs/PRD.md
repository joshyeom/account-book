# PRD: AI 가계부 v2.0

## 문서 정보

| 항목        | 내용            |
| ----------- | --------------- |
| 버전        | 2.1             |
| 작성일      | 2026-01-24      |
| 상태        | Draft           |
| 대상 사용자 | 자산관리 초보자 |

---

## 1. 제품 개요

### 1.1 제품 비전

**"스크린샷 한 장으로 시작하는 똑똑한 가계부"**

자산관리를 처음 시작하는 사용자가 복잡한 설정 없이 스크린샷 또는 직접 입력으로 지출/수입을 관리하고, 월 예산 대비 사용 현황을 한눈에 파악할 수 있는 심플한 가계부 앱.

### 1.2 핵심 가치

| 가치       | 설명                                         |
| ---------- | -------------------------------------------- |
| **간편함** | 스크린샷 업로드 또는 직접 입력으로 빠른 기록 |
| **명확함** | 월 예산 대비 남은 금액을 직관적으로 표시     |
| **단순함** | 자산관리 초보자를 위한 필수 기능만 제공      |
| **자동화** | 고정 지출은 매달 자동 반영                   |

### 1.3 대상 사용자

- 자산관리를 처음 시작하는 2030 세대
- 복잡한 가계부 앱에 지쳐 포기한 경험이 있는 사용자
- 스마트폰 뱅킹/카드앱 스크린샷으로 간편하게 기록하고 싶은 사용자

---

## 2. 사용자 문제 및 해결책

### 2.1 문제 정의

| 문제                               | 현재 상태              | 해결책                         |
| ---------------------------------- | ---------------------- | ------------------------------ |
| 카메라 촬영 버튼이 불필요함        | 카메라/갤러리 2개 버튼 | 갤러리 선택 버튼만 유지        |
| 예산 대비 지출 현황을 알 수 없음   | 지출 합계만 표시       | 월 예산 설정 및 잔여 예산 표시 |
| 처음 시작할 때 기준점이 없음       | 바로 지출 입력 시작    | 온보딩에서 월 예산 설정        |
| 스크린샷 없이 입력할 수 없음       | AI 분석만 지원         | 직접 입력 기능 추가            |
| 매달 반복되는 지출 재입력 번거로움 | 매번 수동 입력         | 고정 지출 자동 반영 기능       |

### 2.2 비목표 (Non-Goals)

다음 기능은 **자산관리 초보자**를 위해 의도적으로 제외:

- 총 자산/부채 관리
- 여러 계좌 연동
- 복잡한 예산 카테고리별 분배
- 투자 포트폴리오 추적
- ~~정기 결제/구독 관리~~ → **고정 지출로 단순화하여 포함**

---

## 3. 기능 명세

### 3.1 온보딩 플로우 (신규)

#### 3.1.1 월 예산 설정 화면

**화면 구성:**

```
┌─────────────────────────────────┐
│                                 │
│     💰 이번 달 예산을 설정해주세요   │
│                                 │
│  ┌─────────────────────────┐    │
│  │    ₩ 1,000,000         │    │
│  └─────────────────────────┘    │
│                                 │
│  빠른 선택:                      │
│  ┌───┐ ┌───┐ ┌───┐ ┌───┐       │
│  │50만│ │100만│ │150만│ │200만│   │
│  └───┘ └───┘ └───┘ └───┘       │
│                                 │
│  💡 매달 1일에 예산이 초기화됩니다   │
│                                 │
│  ┌─────────────────────────┐    │
│  │        시작하기          │    │
│  └─────────────────────────┘    │
│                                 │
│        나중에 설정하기 →          │
└─────────────────────────────────┘
```

**기능 요구사항:**

| ID     | 요구사항                                   | 우선순위 |
| ------ | ------------------------------------------ | -------- |
| ONB-01 | 숫자 키패드로 직접 입력 가능               | P0       |
| ONB-02 | 빠른 선택 버튼 (50만, 100만, 150만, 200만) | P1       |
| ONB-03 | "나중에 설정하기" 선택 시 홈으로 이동      | P0       |
| ONB-04 | 예산 미설정 시 홈에서 설정 유도 배너 표시  | P1       |

### 3.2 홈 화면 개선

#### 3.2.1 예산 현황 카드

**화면 구성:**

```
┌─────────────────────────────────┐
│  1월 예산                        │
│  ━━━━━━━━━━━━━━━━━━ 70%         │
│                                 │
│  ₩700,000 / ₩1,000,000          │
│  남은 예산: ₩300,000             │
│                                 │
│  📊 하루 평균 ₩43,000 사용 가능   │
└─────────────────────────────────┘
```

**기능 요구사항:**

| ID     | 요구사항                                       | 우선순위 |
| ------ | ---------------------------------------------- | -------- |
| HOM-01 | 월 예산 대비 지출 비율을 프로그레스 바로 표시  | P0       |
| HOM-02 | 남은 예산 금액 표시                            | P0       |
| HOM-03 | 남은 일수 기준 하루 사용 가능 금액 계산        | P1       |
| HOM-04 | 예산 초과 시 빨간색으로 경고 표시              | P0       |
| HOM-05 | 예산 미설정 시 "예산을 설정해주세요" 버튼 표시 | P0       |

### 3.3 스크린샷 업로드 개선

#### 3.3.1 단일 버튼 UI

**Before (현재):**

```
┌─────────────┐  ┌─────────────┐
│  📷 카메라   │  │  🖼 갤러리   │
│    촬영     │  │    선택     │
└─────────────┘  └─────────────┘
```

**After (개선):**

```
┌─────────────────────────────────┐
│                                 │
│         🖼️                      │
│     스크린샷 업로드              │
│                                 │
│  결제 내역이나 거래 내역         │
│  스크린샷을 선택해주세요          │
│                                 │
└─────────────────────────────────┘
```

**기능 요구사항:**

| ID     | 요구사항                            | 우선순위 |
| ------ | ----------------------------------- | -------- |
| UPL-01 | 카메라 촬영 버튼 제거               | P0       |
| UPL-02 | 갤러리 선택 버튼만 유지 (크게 표시) | P0       |
| UPL-03 | 드래그 앤 드롭 지원 (웹)            | P2       |

### 3.4 직접 입력 기능 (신규)

#### 3.4.1 입력 방식 선택

**화면 구성:**

```
┌─────────────────────────────────┐
│  거래 추가                       │
├─────────────────────────────────┤
│                                 │
│  ┌─────────────┐ ┌─────────────┐│
│  │ 🖼️ 스크린샷  │ │ ✏️ 직접입력  ││
│  │    분석     │ │            ││
│  └─────────────┘ └─────────────┘│
│                                 │
└─────────────────────────────────┘
```

#### 3.4.2 직접 입력 폼

**화면 구성:**

```
┌─────────────────────────────────┐
│  ✏️ 직접 입력                    │
├─────────────────────────────────┤
│                                 │
│  거래 유형                       │
│  ┌──────────┐ ┌──────────┐      │
│  │ 💸 지출   │ │ 💰 수입   │      │
│  └──────────┘ └──────────┘      │
│                                 │
│  금액                           │
│  ┌─────────────────────────┐    │
│  │ ₩ 0                     │    │
│  └─────────────────────────┘    │
│                                 │
│  항목명                         │
│  ┌─────────────────────────┐    │
│  │ 예: 점심 식사            │    │
│  └─────────────────────────┘    │
│                                 │
│  카테고리                       │
│  ┌───┐ ┌───┐ ┌───┐ ┌───┐       │
│  │식비│ │교통│ │카페│ │쇼핑│ ...   │
│  └───┘ └───┘ └───┘ └───┘       │
│                                 │
│  날짜                           │
│  ┌─────────────────────────┐    │
│  │ 📅 2026년 1월 24일       │    │
│  └─────────────────────────┘    │
│                                 │
│  ☐ 고정 지출로 등록 (매월 반복)   │
│                                 │
│  ┌─────────────────────────┐    │
│  │         저장하기          │    │
│  └─────────────────────────┘    │
└─────────────────────────────────┘
```

**기능 요구사항:**

| ID     | 요구사항                           | 우선순위 |
| ------ | ---------------------------------- | -------- |
| MAN-01 | 지출/수입 유형 선택                | P0       |
| MAN-02 | 금액 입력 (숫자 키패드)            | P0       |
| MAN-03 | 항목명 입력                        | P0       |
| MAN-04 | 카테고리 선택 (기존 카테고리 활용) | P0       |
| MAN-05 | 날짜 선택 (기본값: 오늘)           | P0       |
| MAN-06 | 고정 지출 체크박스                 | P0       |
| MAN-07 | 입력 후 홈으로 이동                | P0       |

### 3.5 고정 지출 관리 (신규)

#### 3.5.1 고정 지출이란?

매달 반복되는 지출로, 한 번 등록하면 매월 자동으로 지출 내역에 반영됩니다.

**예시:** 월세, 통신비, 구독료, 보험료 등

#### 3.5.2 고정 지출 목록

**화면 구성 (설정 페이지 내):**

```
┌─────────────────────────────────┐
│  🔄 고정 지출                    │
├─────────────────────────────────┤
│                                 │
│  ┌─────────────────────────────┐│
│  │ 🏠 월세          ₩500,000   ││
│  │    매월 25일              [x]││
│  └─────────────────────────────┘│
│                                 │
│  ┌─────────────────────────────┐│
│  │ 📱 통신비         ₩65,000   ││
│  │    매월 15일              [x]││
│  └─────────────────────────────┘│
│                                 │
│  ┌─────────────────────────────┐│
│  │ 🎵 스포티파이     ₩10,900   ││
│  │    매월 1일               [x]││
│  └─────────────────────────────┘│
│                                 │
│  ┌─────────────────────────────┐│
│  │      + 고정 지출 추가        ││
│  └─────────────────────────────┘│
│                                 │
│  💡 고정 지출은 매월 해당 날짜에   │
│     자동으로 지출에 추가됩니다    │
└─────────────────────────────────┘
```

#### 3.5.3 고정 지출 등록 폼

**화면 구성:**

```
┌─────────────────────────────────┐
│  🔄 고정 지출 등록               │
├─────────────────────────────────┤
│                                 │
│  항목명                         │
│  ┌─────────────────────────┐    │
│  │ 예: 월세, 통신비         │    │
│  └─────────────────────────┘    │
│                                 │
│  금액                           │
│  ┌─────────────────────────┐    │
│  │ ₩ 0                     │    │
│  └─────────────────────────┘    │
│                                 │
│  카테고리                       │
│  ┌───┐ ┌───┐ ┌───┐ ┌────┐      │
│  │주거│ │공과금│ │여가│ │쇼핑│ ...  │
│  └───┘ └───┘ └───┘ └────┘      │
│                                 │
│  결제일                         │
│  ┌─────────────────────────┐    │
│  │ 매월  [25] 일            │    │
│  └─────────────────────────┘    │
│                                 │
│  ┌─────────────────────────┐    │
│  │         등록하기          │    │
│  └─────────────────────────┘    │
└─────────────────────────────────┘
```

**기능 요구사항:**

| ID     | 요구사항                                         | 우선순위 |
| ------ | ------------------------------------------------ | -------- |
| FIX-01 | 고정 지출 등록 (항목명, 금액, 카테고리, 결제일)  | P0       |
| FIX-02 | 고정 지출 목록 조회                              | P0       |
| FIX-03 | 고정 지출 수정                                   | P0       |
| FIX-04 | 고정 지출 삭제                                   | P0       |
| FIX-05 | 매월 결제일에 자동으로 expenses에 추가           | P0       |
| FIX-06 | 고정 지출에서 생성된 건 표시 (🔄 아이콘)         | P1       |
| FIX-07 | 이번 달 고정 지출 합계를 예산에서 미리 차감 표시 | P1       |

### 3.6 설정 페이지

#### 3.6.1 예산 관리

**화면 구성:**

```
┌─────────────────────────────────┐
│  💰 월 예산 설정                 │
│  ─────────────────────────────  │
│  현재 예산: ₩1,000,000          │
│                           [수정] │
│                                 │
│  📅 예산 초기화일: 매월 1일       │
│                           [수정] │
└─────────────────────────────────┘
```

**기능 요구사항:**

| ID     | 요구사항                           | 우선순위 |
| ------ | ---------------------------------- | -------- |
| SET-01 | 월 예산 수정 기능                  | P0       |
| SET-02 | 예산 초기화일 변경 (1일 ~ 28일)    | P2       |
| SET-03 | 예산 변경 히스토리는 저장하지 않음 | -        |

---

## 4. 데이터 모델

### 4.1 신규 테이블: user_settings

```sql
CREATE TABLE user_settings (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL UNIQUE REFERENCES auth.users(id) ON DELETE CASCADE,
  monthly_budget INTEGER DEFAULT NULL,
  budget_reset_day INTEGER DEFAULT 1 CHECK (budget_reset_day >= 1 AND budget_reset_day <= 28),
  onboarding_completed BOOLEAN DEFAULT false,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc', NOW()),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc', NOW())
);

-- RLS
ALTER TABLE user_settings ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view their own settings"
  ON user_settings FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Users can insert their own settings"
  ON user_settings FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update their own settings"
  ON user_settings FOR UPDATE USING (auth.uid() = user_id);
```

### 4.2 신규 테이블: recurring_expenses (고정 지출)

```sql
CREATE TABLE recurring_expenses (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  category_id UUID REFERENCES categories(id) ON DELETE SET NULL,
  name TEXT NOT NULL,
  amount INTEGER NOT NULL,
  day_of_month INTEGER NOT NULL CHECK (day_of_month >= 1 AND day_of_month <= 28),
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc', NOW()),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT TIMEZONE('utc', NOW())
);

-- Index
CREATE INDEX idx_recurring_expenses_user_id ON recurring_expenses(user_id);

-- RLS
ALTER TABLE recurring_expenses ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view their own recurring expenses"
  ON recurring_expenses FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Users can create their own recurring expenses"
  ON recurring_expenses FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update their own recurring expenses"
  ON recurring_expenses FOR UPDATE USING (auth.uid() = user_id);

CREATE POLICY "Users can delete their own recurring expenses"
  ON recurring_expenses FOR DELETE USING (auth.uid() = user_id);
```

### 4.3 expenses 테이블 수정

```sql
-- 기존 expenses 테이블에 컬럼 추가
ALTER TABLE expenses
  ADD COLUMN recurring_expense_id UUID REFERENCES recurring_expenses(id) ON DELETE SET NULL;

-- Index
CREATE INDEX idx_expenses_recurring ON expenses(recurring_expense_id);
```

### 4.4 필드 설명

**user_settings:**

| 필드                 | 타입    | 설명                         |
| -------------------- | ------- | ---------------------------- |
| monthly_budget       | INTEGER | 월 예산 (원 단위, null 허용) |
| budget_reset_day     | INTEGER | 예산 초기화일 (1-28, 기본 1) |
| onboarding_completed | BOOLEAN | 온보딩 완료 여부             |

**recurring_expenses:**

| 필드         | 타입    | 설명                                |
| ------------ | ------- | ----------------------------------- |
| name         | TEXT    | 고정 지출 항목명                    |
| amount       | INTEGER | 금액 (원)                           |
| day_of_month | INTEGER | 결제일 (1-28)                       |
| is_active    | BOOLEAN | 활성화 여부 (비활성화 시 생성 안함) |

**expenses (추가 필드):**

| 필드                 | 타입 | 설명                                   |
| -------------------- | ---- | -------------------------------------- |
| recurring_expense_id | UUID | 고정 지출에서 생성된 경우 해당 ID 연결 |

---

## 5. 사용자 플로우

### 5.1 신규 사용자 플로우

```
┌──────────┐
│  로그인   │
└────┬─────┘
     ▼
    ◇ 온보딩
   ╱완료?╲
  ╱      ╲
FALSE    TRUE
  │        │
  ▼        │
┌──────────┐  │
│ 예산 설정 │  │
│  온보딩  │  │
└────┬─────┘  │
     │        │
     ├────────┘
     ▼
┌──────────┐
│    홈    │
└──────────┘
```

### 5.2 지출 등록 플로우

```
┌──────────┐
│    홈    │
└────┬─────┘
     ▼
┌───────────────┐
│  + 버튼 클릭   │
└────┬──────────┘
     ▼
    ◇ 입력방식
   ╱  선택  ╲
  ╱          ╲
스크린샷    직접입력
  │            │
  ▼            ▼
┌───────────┐ ┌───────────┐
│ 갤러리    │ │ 입력 폼   │
│   선택    │ │   작성    │
└────┬──────┘ └────┬──────┘
     │             │
     ▼             │
┌───────────┐      │
│ AI 분석   │      │
└────┬──────┘      │
     │             │
     ▼             │
┌───────────┐      │
│결과 확인  │      │
│  /수정    │      │
└────┬──────┘      │
     │             │
     └──────┬──────┘
            ▼
      ┌───────────┐
      │    저장    │
      └────┬──────┘
           ▼
      ┌──────────┐
      │   홈     │ ← 예산 현황 업데이트
      └──────────┘
```

### 5.3 고정 지출 자동 생성 플로우

```
┌─────────────────┐
│ 매일 자정 (Cron) │
└────────┬────────┘
         ▼
   ◇ 오늘 = 결제일?
  ╱            ╲
YES            NO
 │              │
 ▼              ▼
┌─────────┐   (종료)
│활성화된  │
│고정지출  │
│  조회   │
└────┬────┘
     ▼
┌─────────┐
│expenses │
│테이블에 │
│자동 추가│
└────┬────┘
     ▼
  (종료)
```

---

## 6. UI/UX 가이드라인

### 6.1 예산 상태 색상

| 상태 | 색상   | 조건             |
| ---- | ------ | ---------------- |
| 안전 | Green  | 사용률 0-70%     |
| 주의 | Yellow | 사용률 71-90%    |
| 위험 | Orange | 사용률 91-100%   |
| 초과 | Red    | 사용률 100% 초과 |

### 6.2 핵심 UX 원칙

1. **원탭 액션**: 주요 기능은 1-2탭 내 완료
2. **즉각적 피드백**: 예산 변화는 실시간 반영
3. **비판단적 톤**: "초과했습니다" 대신 "예산을 넘었어요"
4. **긍정적 강화**: 예산 내 사용 시 격려 메시지

---

## 7. 마일스톤

### Phase 1: MVP (v2.0)

| 기능                      | 우선순위 | 예상 기간 |
| ------------------------- | -------- | --------- |
| 온보딩 - 월 예산 설정     | P0       | 2일       |
| 홈 - 예산 현황 카드       | P0       | 2일       |
| 업로드 - 카메라 버튼 제거 | P0       | 0.5일     |
| 설정 - 예산 수정          | P0       | 1일       |
| DB 스키마 추가            | P0       | 0.5일     |
| **직접 입력 기능**        | P0       | 2일       |
| **고정 지출 관리**        | P0       | 3일       |

**총 예상 기간: 11일**

### Phase 2: 개선 (v2.1)

| 기능                          | 우선순위 |
| ----------------------------- | -------- |
| 예산 초기화일 커스텀          | P2       |
| 하루 사용 가능 금액 알림      | P2       |
| 주간 리포트                   | P2       |
| 고정 지출 예산 미리 차감 표시 | P1       |

---

## 8. 성공 지표

| 지표                   | 목표    | 측정 방법                      |
| ---------------------- | ------- | ------------------------------ |
| 온보딩 완료율          | > 80%   | 예산 설정 완료 사용자 비율     |
| 월간 활성 사용자 (MAU) | -       | 월 1회 이상 지출 등록          |
| 평균 등록 지출 건수    | 15건/월 | 사용자당 월평균 지출 등록 수   |
| 고정 지출 등록률       | > 50%   | 1개 이상 고정 지출 등록 사용자 |
| 직접 입력 사용률       | > 30%   | 직접 입력으로 등록된 거래 비율 |

---

## 9. 기술 고려사항

### 9.1 고정 지출 자동 생성

**옵션 A: Supabase Edge Function + pg_cron**

- Supabase 내장 cron으로 매일 자정 실행
- 서버리스, 추가 인프라 불필요

**옵션 B: Next.js API Route + Vercel Cron**

- Vercel cron job으로 API 호출
- vercel.json에 cron 설정

**권장:** 옵션 A (Supabase pg_cron)

```sql
-- 예시: 매일 자정 실행
SELECT cron.schedule(
  'generate-recurring-expenses',
  '0 0 * * *',  -- 매일 00:00
  $$
    INSERT INTO expenses (user_id, category_id, name, amount, type, date, recurring_expense_id)
    SELECT
      user_id,
      category_id,
      name,
      amount,
      'expense',
      CURRENT_DATE,
      id
    FROM recurring_expenses
    WHERE is_active = true
      AND day_of_month = EXTRACT(DAY FROM CURRENT_DATE)
  $$
);
```

### 9.2 고정 지출 중복 방지

같은 날 같은 고정 지출이 중복 생성되지 않도록:

```sql
-- expenses 테이블에 unique constraint 추가
CREATE UNIQUE INDEX idx_unique_recurring_per_day
  ON expenses(recurring_expense_id, date)
  WHERE recurring_expense_id IS NOT NULL;
```

---

## 변경 이력

| 버전 | 날짜       | 변경 내용                                  |
| ---- | ---------- | ------------------------------------------ |
| 2.0  | 2026-01-24 | 초안 작성 - 카메라 제거, 월 예산 기능 추가 |
| 2.1  | 2026-01-24 | 직접 입력 기능, 고정 지출 관리 기능 추가   |
